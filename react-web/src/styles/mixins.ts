// Web version of the scaling function
export const scaleFont = (size: number): number => {
  // For web, we'll use a simpler approach with CSS media queries for responsiveness
  // This function returns the base size, and we'll handle scaling in CSS
  return size;
};

// Helper function to scale images based on screen size
export const scaleImage = (size: number): number => {
  // In web, we can use CSS for responsive images
  // This is just a placeholder for compatibility
  return size;
};
