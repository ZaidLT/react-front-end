/**
 * Hexagon hover animation types
 */
export enum HexHoverType {
  Scale = 'scale',
  // Future: Pulse, Glow, etc.
}

/**
 * Hexagon background variant types
 */
export enum HexBackgroundType {
  Light = 'light',
  Gradient = 'gradient',
}
